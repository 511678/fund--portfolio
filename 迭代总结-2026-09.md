# 基金板块分析工具 · 迭代总结

> 周期：2026-09-04 ~ 2026-09-14 ｜ 版本演进：a7ef162 → v12
> 线上：https://511678.github.io/fund--portfolio/ ｜ 仓库：`511678/fund--portfolio`（双横线）
> 本地源码：`基金/fund-portfolio/` ｜ 本地服务：http://localhost:8377/

---

## 一、项目现状一览

个人基金看板，**手机/电脑双端可用、零成本托管**（GitHub Pages + Actions），无需服务器、无需数据库。

| 能力 | 说明 |
|---|---|
| 持仓账本 | 20 只基金全量收录；市值/成本/已落袋/手选板块 |
| 真实盈亏 | 持有收益（市值−成本）+ 已落袋（卖出落袋）= 累计盈亏 +¥524.39 |
| 板块分析 | 10 大板块自动归类 + 1000 个东财细分板块手选覆盖 |
| 基准对比 | 组合净值走势 vs 沪深300（今年以来 +43.2% vs -4.4%），滑动/悬停查看逐日状态 |
| 每日收益 | 近 14 天 基金×日期 矩阵（逐日回折精确口径） |
| 大事日历 | 美联储全年 8 次 + CPI 12 次 + 日本/韩国央行 + 龙头财报（官方源核实），全年回看横滑卡片 |
| 录入提效 | 代码自动查名 + 板块自动识别（名称快判→重仓穿透）+ 截图/语音录入（等智谱 Key） |
| 多端同步 | 备份文件导入 / `?data=` 一键同步链接 |

---

## 二、系统架构

```
浏览器（手机/电脑，无构建无依赖）
  index.html        视图 / 样式 / 交互（单文件 ~1400 行）
  js/core.js        纯逻辑层：板块规则、esc 转义、日期、账本算法
                    浏览器挂 FP 全局 / Node 端 module.exports（测试用）
  localStorage      fp_amounts（账本）/ fp_txns / fp_settings / fp_events / fp_market_cache
      │ fetch（全部使用 CORS 全开的公开接口）
      ├─ fundmobapi.eastmoney.com   基金详情 / 盘中估值 / 前十大重仓
      ├─ push2delay.eastmoney.com   个股申万行业 / 板块列表（1000 个）
      ├─ web.ifzq.gtimg.cn          沪深300 日线基准（腾讯，稳定）
      └─ open.bigmodel.cn           截图视觉识别（免费模型，等 Key）

GitHub 仓库 = 数据库 + CI + 托管
  ├─ data/funds.json    基金清单（20 只，公开，手机可写）
  ├─ data/market.json   行业构成 / 今年以来净值（169 天）/ 沪深300 基准
  ├─ data/sectors.json  板块列表快照（前端实时拉取失败时的兜底）
  ├─ data/events.json   大事日历（官方核实日程）
  ├─ Actions·update-market-data：交易日 22:30 自动抓数并提交（push funds.json 也会触发）
  ├─ Actions·tests：Node 24 跑 66 项测试
  └─ Pages：线上部署（页脚版本号核对新旧）

本地开发
  scripts/serve.py       禁缓存本地服务器（8377 端口）
  ~/.zshrc 自启块        开终端自动检查并拉起服务
  scripts/update_data.py 手动抓数（python3 scripts/update_data.py）
```

### 关键设计决策

1. **无构建原则**：所有依赖是浏览器原生能力 + 公开接口，任何一台手机打开网址就是完整应用。代价是单文件偏大，收益是十年后还能打开。
2. **纯逻辑层分离**（js/core.js）：板块归类、账本算法、日期/转义等与 DOM 无关的代码独立成文件，浏览器与 Node 测试双端共用——这是测试能落盘的前提。
3. **数据分层**：公开数据（清单/行情/日历）进 git 仓库由 Action 维护；私有数据（金额/成本）只存本机 localStorage，导出走备份文件或同步链接。
4. **账本模型**：
   ```
   amounts[code] = {
     amt:      市值快照（自动按净值滚动到最新交易日）
     cost:     累计投入成本（null = 未录，不参与盈亏、不显示误导性 0）
     d:        快照对应净值日（滚动基准）
     sector:   手选细分板块（优先级：手选 > 真实行业数据 > 兜底）
     realized: 已实现盈亏（卖出落袋 = 卖出所得 − 卖出部分成本）
   }
   每只基金的累计收益（App 口径）= 持有收益(amt−cost) + realized
   ```
5. **导入语义**：外部表格的「收益」列 = App 累计收益，入库自动拆分（有成本补差额进已落袋；无成本整体进已落袋）——确保总账恒等于用户在 App 看到的数字。

---

## 三、问题与修复记录（现象 → 根因 → 做法）

### 安全类

| 问题 | 根因 | 修复 |
|---|---|---|
| 截图识别结果直插 HTML 属性，可注入脚本 | LLM/语音输出是不可信输入，却直接拼进 `innerHTML`/`value` | 全站统一 `esc()` 转义（10 个注入点逐一清点）；确认弹窗属性转义 |
| 删除按钮内联 `onclick="delFund('${c}')"` | 代码来自用户输入框/导入 JSON，引号可逃逸 JS 上下文 | 改为 `data-*` + 事件委托，消除内联 JS |
| 备份导入不设防 | 恶意 JSON 直接覆盖 localStorage | 边界清洗：代码强制 6 位、金额/名称/动作校验截断 |
| 出错静默（页面白屏无从排查） | 无全局错误处理 | `onerror`/`unhandledrejection` 显示到页面顶部徽标（这次排障立了大功） |

### 数据正确性类

| 问题 | 根因 | 修复 |
|---|---|---|
| 「总投入成本」显示的是市值合计 | 没有成本模型 | Sprint 1 引入 cost 字段 + 真实盈亏 |
| 卖出后收益凭空消失 | 无已实现概念：全卖后 市值0−成本0=0 | realized 字段：落袋 = 卖出所得 − 摊减成本 |
| 清仓基金收益不计总账 | Hero 循环只扫持仓中（amt>0）的基金 | 总账扫全部基金；全清仓不再走空状态 |
| 超卖虚增落袋 | 卖出额可大于市值 | 卖出额 clamp 到市值 |
| 未录成本被算成 0 成本（假盈利） | `cost \|\| 0` 写法 | null 语义贯穿：未录就不算，绝不造数 |
| 「已赚/亏」列重复计入 | 用户填的是 App 累计收益，系统当已落袋再算一次 | 导入语义修正：按 App 口径自动拆分（总账恒等 App 数字） |
| 收益金额小数被抹掉 | `fmtY` 用整数格式化 | 统一保留两位小数 |
| 录入日期凌晨/深夜差一天 | `toISOString()` 是 UTC | 全部改本地时区 `todayLocal()` |
| 停更数据冒充「今日」 | 静态净值回退链无时效校验 | 4 个日历日窗口（日历日差，非时间差——测试抓到的边界） |
| 市值快照过期失真 | 快照不随净值滚动 | `syncValues()` 逐日滚动落库；旧数据按清单 added 日期迁移 |

### 崩溃与稳定性类

| 问题 | 根因 | 修复 |
|---|---|---|
| **总览整页卡死「数据加载中…」**（用户实报） | `compute()` 裸访问 `market.funds[c].nav_history`——新录基金在 Action 抓数前不在 market.json，只要组合里同时有一只有数据的基金必崩 | 加 `(market.funds[c] \|\| {})` 防护 + 回归测试 |
| 刷新后仍是旧版页面/新旧混跑 | SimpleHTTP 无缓存头 → 浏览器启发式缓存 | 自建 serve.py 加 `Cache-Control: no-store`；脚本引用加版本参数 `core.js?v=`；页脚版本号肉眼可辨 |
| 服务器无响应时永久卡加载 | fetch 无超时 | 8 秒 AbortController + localStorage 缓存兜底 |
| 服务器进程随会话退出而死 | nohup 进程生命周期 | .zshrc 自启动块（幂等）；LaunchAgent 方案因 macOS TCC 不放行而放弃（教训：launchd 进程无「文稿」夹权限） |
| 手机上 7 列表格被截断 | 无滚动容器 | `.tbl-scroll { overflow-x:auto }` |

### 工程与流程类

| 问题 | 根因 | 修复 |
|---|---|---|
| 「29 条单测」只活在对话里 | 测试从未落盘 | tests/ 三套文件 66 项，进 GitHub Actions |
| CI 挂而本地绿 | Node 20 缺 jsdom 需要的新版 undici API | CI 升 Node 24（本地先用 Node 24 二进制复现验证再改） |
| jsdom 测试隐式依赖本地服务器 | `<script src>` 走网络加载 | core.js 从磁盘读入后 `window.eval` 注入，测试完全自包含（CI 可复现） |
| 推送冲突 | Action 自动提交 market.json | 惯例：`git pull --rebase` 再推；冲突时数据文件取最新本地版 |
| **update_data.py 被写坏**（9286 个函数副本） | Python 脚本里 `s.replace("", new)` 空串替换灾难 | git checkout 秒恢复。教训：**脚本化修改文件后必须立即语法检查**（`ast.parse`/`node --check`） |
| 东财行情域对脚本断连（push2his 拒 urllib、间歇拒 curl） | 反爬 | 基准换腾讯财经源；板块列表走 push2delay + 桌面 UA |
| YTD 年份取错（用了数据首年） / 基准基点吃掉起点日涨跌 / funds 参数未透传 | 三个独立逻辑 bug | 全部被新写的测试当场抓住（测试落盘的价值实证） |
| 大事日历测试 fixture 旧 / 测试 ROOT 路径少一层 `..` / 浮点 -0 严格相等 | 测试自身三处笔误 | 逐个修正——测试代码同样需要 review |
| 走势交互浮层需要逐日数据 | compute 未暴露日收益序列 | compute 返回 pr/prDates 供交互层使用 |
| 测试浮点严格相等、fixture 日期倒序、ROOT 路径少一层 | 测试自身笔误 | 逐个修正——测试也要 review |

---

## 四、功能迭代史

| 迭代 | 内容 | 提交 |
|---|---|---|
| Sprint 0 止血 | XSS 全量转义、本地日期、净值时效、未披露单列、口径标注、测试进 CI | `6c0b6e7` |
| Sprint 1 账本 | cost/cost 开账/买卖矩阵/市值滚动、Hero 累计盈亏、明细持有收益列、成本编辑 | `d99584e` |
| 总览修复 | 无 PAT 录入后总览空白（renderDash 条件刷新） | `88b0d01` |
| 已实现盈亏 | 卖出落袋不清零、Hero 拆分「持有 ｜ 已落袋」、✎ 已落袋补录 | `af415c2` |
| 稳定性包 | 总览崩溃修复、缓存治理、加载超时、表格滚动、错误可见化 | `31dd3b2` `8a315b6` |
| 数据收录 | 用户 Numbers 表格 → 逐只接口验证（修正 178111→017811）→ 20 只入库 | `933b29a` `8be7e96` |
| 板块选择器 | 1000 个东财板块搜索手选，覆盖自动归类 | `1a42476` |
| 自动识别 | 名称关键词快判 → 重仓股穿透投票，录入免手选 | `f6220ef` |
| Sprint 2 明细 | 每日收益矩阵（逐日回折）、排序切换、弹窗自动识别板块 | `af69d0d` |
| Sprint 3 观测 | 沪深300 基准线、今年以来 KPI、未披露拆解、补成本引导 | `aa64a8c` |
| 盈亏口径修正 | 导入收益列 = App 累计收益自动拆分 | `9a2892c` |
| 一键同步 | `?data=<base64>` 点链接即导入（手机/电脑通用） | `223e0ec` |
| 大事日历 | FOMC/日本央行/韩国央行/CPI/非农/LPR/PMI/龙头财报，横滑卡片带 | `261f70a` `9500678` |
| 日历全年化 | FOMC 全年 8 次 + CPI 全年 12 次官方日期内置，视界改「今年 1/1 起全年回看」 | `5677a9d` |
| 走势图交互 | 滑动/悬停逐日浮层：日期、组合指数与涨跌%、基准指数；竖线+双圆点跟随；手机横滑不干扰页面滚动 | `5677a9d` |
| **PM 整体验收** | 七项验收（仓库/测试 68 项/CI/线上部署/数据对账精确一致/安全审计/六视图 34 项功能断言）全部通过 | — |

### 大事日历信息源清单（准确性分级）

| 事件 | 信息源 | 精确度 |
|---|---|---|
| 美联储 FOMC（全年 8 次） | federalreserve.gov 官方日历 | 精确 |
| 美国 CPI（全年 12 次发布日） | bls.gov + FRED 逐月核实 | 精确 |
| 日本央行 MPM（3 次） / 韩国央行（2 次） | boj.or.jp / bok.or.kr | 精确 |
| 非农（每月首个周五）/ 中国 LPR（每月 20 日）/ PMI（月末） | 规则性日程，程序生成 | 规则级 |
| 龙头财报（英伟达 11/17 已确认；微软/苹果/台积电等标「预计」） | Wall Street Horizon / 公司 IR 惯例 | 窗口级 |

### PM 整体验收报告（2026-09-14）

**结论：✅ 通过，无保留意见。** 七项核对全部通过：

1. 仓库：工作区干净，本地 = 远程（`5677a9d`），31 个迭代提交全部推送
2. 测试：68/68 全绿
3. CI：tests（Node 24）success、Pages 自动部署 success
4. 线上：页面 + 4 个数据文件 + core.js 全部 200，线上版本 = v12
5. 数据对账：市值 ¥3,388.86、累计盈亏 +524.39 与导入源**精确一致**（差异 0.00）；清单 20 只；日历 33 项事件
6. 安全审计：innerHTML 零裸插值（脚本精确审计）、内联 onclick=0、eval=0
7. 功能实测：总览 13 项 / 明细 8 项 / 录入 7 项 / 设置 6 项断言全过

遗留（不阻断）：智谱 Key、电脑端 PAT、手机端一次同步、events.json 2027 年更新。风险（低）：百只以上基金需优化板块计算；财报「预计」日期以 IR 为准。

---

## 五、工作流（三角色制）

```
甲方（wangxu）提需求/拍板
   ↓
① PM 出任务卡：背景 → 范围 → 验收标准（可勾选）→ 风险；明确「不做」防止范围蔓延
   ↓
② DEV 实现：禁止超范围改动；自测门禁 = 语法检查 + 全量测试 + 浏览器实测
   ↓
③ QA 验收：对照验收标准逐条打勾；金额改动必须「页面数字 vs App 数字」对账；
   innerHTML 新增插值必查转义；每次迭代至少沉淀 1 条自动化测试
   ↓
甲方终审 → git pull --rebase && git push
```

实证案例：Sprint 1 的 isFreshNav 边界、Sprint 3 的 YTD 年份/基准基点，都是测试先于人工发现。

---

## 六、当前待办与已知边界

**等待外部输入**
- 智谱 API Key → 截图识别全链路激活（含自动补成本）
- 电脑端 PAT → 基金清单自动同步（目前手动合并）
- 手机端一次导入同步链接/备份文件（桌面 `基金数据导入-2026-09-13.json` 或 `?data=` 一键同步链接）
- events.json 2027 年更新（美联储/日韩央行新年度日历公布后）

**已知边界（设计取舍，非 bug）**
- 「未披露」桶 33.6%：混合型基金只披露前十大重仓，属数据源特性；可手选细分板块拆解
- 每日收益在买卖当日会有小误差（无申赎假设），已在标题标注口径
- 财报日期标「预计」的以各公司 IR 为准（英伟达 11/17 已确认）；2027 年 FOMC 日历公布后需更新 events.json
- 20 只基金的 compute 为 O(基金²×天数)，百只以上才需要优化

**候选改进**
- 持仓行展开详情（单基金迷你走势/成本明细）
- 已清仓基金专区
- 预警阈值可配置 + 单只占比提醒
- 手机版每日收益改为按天翻页列表
- 大事日历财报精确日期自动抓取（当前为内置 + 惯例窗口）

---

*文档生成于 2026-09-14，最后更新于同日（v12 后），对应线上版本 v12、提交 `5677a9d`，PM 整体验收通过。*
